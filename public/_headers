# Cloudflare Pages headers.
# Hashed assets and the Pagefind index are content-addressed / stable, so cache
# them aggressively. HTML is left to Pages' defaults so content updates are seen.

/_astro/*
  Cache-Control: public, max-age=31536000, immutable

/pagefind/*
  Cache-Control: public, max-age=86400
